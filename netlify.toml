[build]
  base    = "web"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# All routes go to index.html (SPA)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
